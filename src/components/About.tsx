import { FC } from 'react'

interface AboutProps {
  
}

const About: FC<AboutProps> = ({}) => {
  return <div id='about' className='w-full h-full'>
    <div className="flex">
        <div className="w-full h-full">
            about
        </div>
    </div>
  </div>
}

export default About